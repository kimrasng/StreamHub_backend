services:
  redis:
    image: redis:7-alpine
    restart: unless-stopped

  backend:
    build: .
    env_file: .env
    ports:
      - "8000:8000"
    depends_on:
      - redis
    volumes:
      - backend_db:/app/data
    restart: unless-stopped

volumes:
  backend_db:
